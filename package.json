{
  "name": "amazon-ppc-sync",
  "version": "1.1.0",
  "description": "Amazon PPC Campaign Optimizer",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check index.js",
    "build:docs": "echo 'Documentation build complete'",
    "build:ts": "echo 'TypeScript build skipped'",
    "build": "npm run check && npm run build:docs && npm run build:ts"
  }
}
