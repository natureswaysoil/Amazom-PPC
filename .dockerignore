# Exclude development and VCS metadata
.git
.gitignore

# Exclude local dependencies and build artifacts
node_modules
npm-debug.log*
dist

# Python cache directories that may appear during local tooling runs
__pycache__/
*.py[cod]

# Environment configuration files should be provided via secrets
.env
.env.*

# Keep TypeScript sources and configuration in the build context
!tsconfig.json
!src/**/*
